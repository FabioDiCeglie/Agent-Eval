# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# MCP Gateway (required for tasks with tools_allowed)
MCP_GATEWAY_URL=http://localhost:8080
# JWT signed with GATEWAY_JWT_SECRET from the gateway .env (HS256, sub = any client id)
MCP_GATEWAY_TOKEN=
